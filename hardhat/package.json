{
  "name": "crowdfunding-hardhat",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:contract": "node scripts/sync-contract.mjs",
    "compile": "npm run sync:contract && hardhat compile",
    "test": "npm run sync:contract && hardhat test",
    "clean": "hardhat clean",
    "deploy": "npm run sync:contract && hardhat run scripts/deploy.ts",
    "seed:demo": "npm run sync:contract && hardhat run scripts/seed-demo.ts",
    "ignition:deploy": "npm run sync:contract && hardhat ignition deploy ignition/modules/Crowdfunding.ts",
    "campaign:create": "npm run sync:contract && hardhat run scripts/create-campaign.ts",
    "campaign:donate": "npm run sync:contract && hardhat run scripts/donate.ts",
    "campaign:close": "npm run sync:contract && hardhat run scripts/close-campaign.ts",
    "disbursement:request": "npm run sync:contract && hardhat run scripts/create-disbursement-request.ts",
    "disbursement:approve": "npm run sync:contract && hardhat run scripts/approve-disbursement.ts",
    "disbursement:reject": "npm run sync:contract && hardhat run scripts/reject-disbursement.ts",
    "disbursement:cancel": "npm run sync:contract && hardhat run scripts/cancel-disbursement.ts",
    "disbursement:withdraw": "npm run sync:contract && hardhat run scripts/withdraw.ts",
    "balances": "npm run sync:contract && hardhat run scripts/check-balances.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.15",
    "@nomicfoundation/hardhat-ignition": "^3.1.8",
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.7",
    "@types/chai": "^5.2.3",
    "@types/mocha": ">=10.0.10",
    "@types/node": "^22.8.5",
    "chai": "^6.2.2",
    "ethers": "^6.14.0",
    "hardhat": "^3.12.0",
    "mocha": "^11.0.0",
    "typescript": "~6.0.3"
  }
}
