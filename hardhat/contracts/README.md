# Contract source

`Crowdfunding.sol` trong thư mục này được tạo từ `../../SMARTCONTRACT/Smartcontract.sol` bằng `npm run sync:contract`. Hãy sửa contract gốc trong `SMARTCONTRACT/`, không sửa bản sao này.
